#include <stdio.h>
#include "main.h"

/**
 * main - prints the name of the program
 * @argv: array of arguments
 *
 * Return: Always 0 (Success)
 */
int main(char *argv[])
{
    printf("%s\n", argv[0]);

    return (0);
}
